spring:
  main:
    banner-mode: off

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver

  flyway:
    enabled: false

  jpa:
    database: h2
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

server:
  servlet:
    context-path: "/api"
